# Scripts

This directory is reserved for small, reproducible command-line entry points.

Scripts may coordinate configuration loading, experiment execution, validation, or reporting. Reusable scientific logic must remain in `src/veranav` rather than being implemented directly in scripts.

No executable script has been added yet.
