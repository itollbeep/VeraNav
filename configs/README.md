# Experiment Configurations

This directory is reserved for version-controlled VeraNav experiment configurations.

Each configuration must use:

- relative paths
- explicit physical units
- explicit coordinate-frame conventions
- deterministic random seeds
- documented estimator and fault-scenario parameters

Machine-specific absolute paths must not be stored in tracked configuration files.

No experiment configuration has been defined yet.
